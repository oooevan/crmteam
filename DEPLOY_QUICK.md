# Быстрый деплой FROMI CRM

## Вариант 1: Vercel (Самый простой)

### Через веб-интерфейс:

1. Зайдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите "Add New Project"
3. Подключите ваш репозиторий или загрузите папку проекта
4. Vercel автоматически определит Vite
5. Нажмите "Deploy"

**Готово!** Ваше приложение будет доступно по адресу `your-project.vercel.app`

### Через CLI:

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Деплой
vercel --prod
```

---

## Вариант 2: Netlify

### Через веб-интерфейс:

1. Зайдите на [netlify.com](https://netlify.com)
2. Перетащите папку `dist` (после сборки) в окно браузера
3. Или подключите GitHub репозиторий

### Через CLI:

```bash
# Установите Netlify CLI
npm i -g netlify-cli

# Войдите
netlify login

# Деплой
netlify deploy --prod
```

---

## Локальная сборка

Перед деплоем соберите проект:

```bash
# Установите зависимости (если еще не установлены)
npm install

# Соберите проект
npm run build
```

После этого папка `dist` будет содержать готовые файлы для деплоя.

**Если возникает ошибка с .env:**
- Создайте пустой файл `.env` в корне проекта
- Или просто игнорируйте ошибку - сборка все равно пройдет

---

## Важно для продакшна

1. **Supabase настройки:** Убедитесь, что в Supabase Dashboard разрешены домены вашего деплоя (CORS)
2. **Realtime:** Должен работать автоматически
3. **Данные:** Все данные хранятся в Supabase, поэтому будут доступны на любом домене

---

## После деплоя

1. Проверьте, что приложение открывается
2. Войдите как таргетолог
3. Проверьте, что данные загружаются из Supabase
4. Проверьте, что ввод данных работает

**Готово!** 🚀
