# Исправление Real-time синхронизации

## Проблема: ID не совпадает по типу

В SQL схеме колонка `id` имеет тип `UUID`, но код использует строку `'main-reports'`. Это может быть причиной того, что real-time синхронизация не работает.

## Решение 1: Изменить тип колонки ID на TEXT

Если таблица уже создана, выполни эти SQL команды в Supabase SQL Editor:

```sql
-- 1. Изменить тип колонки id с UUID на TEXT
ALTER TABLE reports ALTER COLUMN id TYPE TEXT;

-- 2. Если в таблице есть запись, обнови ее ID на 'main-reports'
-- (замени UUID на реальный UUID из твоей таблицы)
UPDATE reports SET id = 'main-reports' WHERE id = (SELECT id FROM reports LIMIT 1);

-- ИЛИ если таблица пустая, просто вставь запись:
-- INSERT INTO reports (id, data) VALUES ('main-reports', '{}'::jsonb);
```

**Внимание:** Замени `(SELECT id FROM reports LIMIT 1)` на реальный UUID из твоей таблицы, если там уже есть данные.

## Решение 2: Пересоздать таблицу (если данных нет или они не важны)

Если в таблице нет важных данных, можно пересоздать ее:

```sql
-- 1. Удалить таблицу
DROP TABLE IF EXISTS reports CASCADE;

-- 2. Выполнить весь скрипт из supabase-schema.sql заново
-- (теперь id будет типа TEXT)
```

## После исправления

1. Выполни SQL команды выше
2. Убедись, что Realtime включен:
   ```sql
   ALTER PUBLICATION supabase_realtime ADD TABLE reports;
   ```
3. Обнови страницу в обоих браузерах
4. Проверь консоль - должно появиться:
   - `✅ Подписка на изменения активна`
   - `📡 Получено обновление из Supabase: UPDATE` (при изменении)

## Проверка ID в таблице

1. Открой Supabase Dashboard → Table Editor
2. Открой таблицу `reports`
3. Проверь тип колонки `id`:
   - Должен быть `text` (не `uuid`)
   - Значение должно быть `main-reports`
4. Если тип `uuid`, выполни SQL команды выше
