# Проверка подключения к Supabase

## ✅ Что уже сделано:
1. Таблица `reports` создана в Supabase
2. Файл `.env` настроен с переменными окружения

## 🧪 Способ 1: Запуск приложения (рекомендуется)

1. **Запусти приложение:**
   ```bash
   npm run dev
   ```

2. **Открой браузер** и перейди на адрес (обычно `http://localhost:5173`)

3. **Открой консоль разработчика (F12)** и проверь сообщения:

   ✅ **Успешное подключение:**
   - `📥 Загрузка данных из Supabase...`
   - `✅ Данные успешно загружены из Supabase`
   - ИЛИ `📝 Создание начальной записи в Supabase...` (при первом запуске)
   - `✅ Данные успешно сохранены в Supabase`
   - `🔔 Подписка на изменения данных в реальном времени...`
   - `✅ Подписка на изменения активна`

   ❌ **Ошибки подключения:**
   - `❌ Supabase не настроен!` → проверь `.env` файл
   - `❌ Ошибка при получении данных из Supabase` → проверь URL и ключ
   - `❌ Ошибка при подключении к Supabase` → проверь сеть или доступность Supabase

4. **Проверь сохранение данных:**
   - Войди в приложение как любой пользователь (например, Алена)
   - Внеси изменение в данные (например, измени название проекта)
   - Подожди 1-2 секунды
   - В консоли должно появиться: `✅ Данные успешно обновлены в Supabase`

5. **Проверь синхронизацию:**
   - Открой приложение в другом браузере/вкладке
   - Войди как другой пользователь (например, Денис)
   - В первом окне внеси изменение
   - Во втором окне должно появиться: `📡 Получено обновление из Supabase: UPDATE`
   - Изменение должно появиться автоматически

## 🧪 Способ 2: Проверка через Supabase Dashboard

1. **Открой Supabase Dashboard:**
   - Перейди на [supabase.com/dashboard](https://supabase.com/dashboard)
   - Выбери свой проект

2. **Проверь таблицу reports:**
   - Перейди в **Table Editor**
   - Открой таблицу `reports`
   - Должна быть запись с `id = 'main-reports'`
   - В поле `data` должен быть JSON объект с данными

3. **Проверь логи:**
   - Перейди в **Logs** → **Postgres Logs**
   - Должны быть записи о SELECT и UPDATE запросах к таблице `reports`

## 🧪 Способ 3: Проверка через Supabase SQL Editor

1. **Открой SQL Editor** в Supabase Dashboard

2. **Выполни запрос:**
   ```sql
   SELECT id, created_at, updated_at, jsonb_typeof(data) as data_type
   FROM reports
   WHERE id = 'main-reports';
   ```

3. **Должен вернуться результат:**
   - Запись с `id = 'main-reports'`
   - `data_type = 'object'`
   - Даты создания и обновления

## ❌ Устранение проблем

### "Supabase не настроен"
- Проверь, что файл `.env` существует
- Убедись, что переменные `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` заполнены
- Перезапусти dev сервер после изменения `.env`

### "Ошибка при подключении"
- Проверь правильность URL (должен быть вида `https://xxxxx.supabase.co`)
- Проверь правильность ключа (длинная строка)
- Убедись, что проект в Supabase активен

### "Таблица не существует" или "PGRST116"
- Выполни SQL из `supabase-schema.sql` в SQL Editor
- Проверь, что таблица `reports` создана в Table Editor

### "Данные не сохраняются"
- Проверь политики RLS (Row Level Security)
- Должна быть политика "Allow all operations" для таблицы `reports`
- Или отключи RLS временно для тестирования

### "Real-time синхронизация не работает"
- Убедись, что Realtime включен в Supabase (по умолчанию включен)
- Проверь консоль на ошибки подписки
- Проверь, что подписка создана: `✅ Подписка на изменения активна`

## ✅ Готово!

Если все проверки пройдены успешно, приложение готово к использованию! Данные будут синхронизироваться между всеми браузерами в реальном времени.
