# Тест синхронизации между браузерами

## Пошаговая проверка

### Шаг 1: Открой два разных браузера

**Важно:** Используй разные браузеры (Chrome и Firefox, или Chrome и Safari), НЕ вкладки одного браузера!

### Шаг 2: Войди в оба браузера

- Браузер 1: Войди как **Алена**
- Браузер 2: Войди как **Денис** (или наоборот)

### Шаг 3: Открой консоль в обоих браузерах

Нажми F12 в каждом браузере и перейди на вкладку **Console**

### Шаг 4: Проверь статус подписки

В консоли обоих браузеров должно быть:
- `🔔 Подписка на изменения данных в реальном времени...`
- `📊 Статус подписки: SUBSCRIBED`
- `✅ Подписка на изменения активна`

Если статус НЕ `SUBSCRIBED`, подписка не работает.

### Шаг 5: Внеси изменение в Браузере 1

1. В Браузере 1 (Алена) измени название проекта (например, "Ростов" → "Ростов-на-Дону")
2. Подожди 2-3 секунды
3. В консоли Браузера 1 должно появиться:
   - `💾 Сохранение данных в Supabase...`
   - `✅ Данные успешно сохранены в Supabase`
   - `🔔 Событие должно быть отправлено через Realtime...`

### Шаг 6: Проверь Браузер 2

В консоли Браузера 2 должно появиться:
- `📡 Получено обновление из Supabase: UPDATE`
- `📡 Получены обновленные данные через real-time синхронизацию`
- `📋 Пользователи в новых данных:`

И данные должны обновиться автоматически в интерфейсе.

### Если события НЕ приходят

1. **Проверь Realtime:**
   ```sql
   SELECT * FROM pg_publication_tables 
   WHERE pubname = 'supabase_realtime' 
   AND tablename = 'reports';
   ```
   Должен вернуться результат.

2. **Проверь данные в таблице:**
   - Открой Supabase Dashboard → Table Editor
   - Проверь, что данные действительно обновились
   - Проверь, что `id = 'main-reports'`

3. **Проверь фильтр подписки:**
   - В консоли должно быть: `📡 Фильтр подписки: id=eq.main-reports`
   - Убедись, что ID в таблице точно совпадает

4. **Проверь тип колонки ID:**
   - Если `id` типа UUID, но используется строка `'main-reports'`, это проблема
   - Нужно изменить тип на TEXT или использовать UUID

### Альтернативный тест: вручную изменить данные в Supabase

1. Открой Supabase Dashboard → Table Editor
2. Открой таблицу `reports`
3. Найди запись с `id = 'main-reports'`
4. Измени данные в колонке `data` (например, измени JSON)
5. Сохрани изменения
6. В браузере в консоли должно появиться: `📡 Получено обновление из Supabase: UPDATE`

**Если это событие НЕ приходит** - Realtime не работает или фильтр не совпадает.

### Диагностика

Если синхронизация все еще не работает, проверь:

1. **Статус подписки в консоли:**
   - Должен быть `SUBSCRIBED`
   - Если другой статус - проблема с подключением

2. **ID записи:**
   - Проверь, что в таблице `id = 'main-reports'` (точно такая строка)
   - Проверь тип колонки `id` (должен быть TEXT)

3. **Realtime публикация:**
   - Выполни: `ALTER PUBLICATION supabase_realtime ADD TABLE reports;`
   - Убедись, что команда выполнена успешно

4. **Логи в консоли:**
   - Должны быть логи при сохранении: `✅ Данные успешно сохранены в Supabase`
   - Должны быть логи при получении: `📡 Получено обновление из Supabase`

Если все это есть, но синхронизация не работает, возможна проблема с сетью или настройками Supabase.
