# Быстрый старт - Проверка настройки

## ✅ Что уже сделано:
1. Файл `.env` создан и заполнен
2. Зависимости установлены (`npm install`)

## 🔍 Что нужно проверить:

### 1. SQL схема выполнена в Supabase
Убедись, что выполнил SQL из файла `supabase-schema.sql` в Supabase Dashboard:
- Открой Supabase Dashboard → SQL Editor
- Выполни SQL из `supabase-schema.sql`
- Убедись, что таблица `reports` создана

### 2. Запуск приложения

```bash
npm run dev
```

Приложение должно запуститься на `http://localhost:5173` (или другом порту)

### 3. Проверка работы

1. **Проверь консоль браузера:**
   - Должно быть сообщение: `📥 Загрузка данных из Supabase...`
   - Затем: `✅ Данные успешно загружены из Supabase`
   - Или: `📝 Создание начальной записи в Supabase...`

2. **Проверь синхронизацию:**
   - Открой приложение в двух разных браузерах/вкладках
   - Войди как разные пользователи (например, Алена и Денис)
   - Внеси изменение в одном окне
   - Во втором окне изменение должно появиться автоматически (1-2 секунды)

### 4. Возможные ошибки:

**"Supabase не настроен"**
- Проверь, что в `.env` файле правильно указаны `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
- Перезапусти dev сервер после изменения `.env`

**"Ошибка при подключении к Supabase"**
- Проверь правильность URL и ключа в `.env`
- Убедись, что таблица `reports` создана в Supabase
- Проверь политики RLS в Supabase Dashboard (должна быть политика "Allow all operations")

**"Таблица reports не существует"**
- Выполни SQL из `supabase-schema.sql` в SQL Editor

## 🎉 Готово!

Если все работает, данные будут синхронизироваться между всеми браузерами в реальном времени!
