# Настройка Realtime синхронизации в Supabase

## Проблема
Данные сохраняются в Supabase и остаются при перезагрузке страницы, но не синхронизируются между браузерами в реальном времени.

## Решение

### 1. Включение Realtime для таблицы reports

В Supabase Dashboard выполни следующую SQL команду в **SQL Editor**:

```sql
-- Включение Realtime для таблицы reports
ALTER PUBLICATION supabase_realtime ADD TABLE reports;
```

**Важно:** Если таблица уже была создана до этого шага, нужно выполнить эту команду отдельно.

### 2. Проверка включения Realtime

1. Открой Supabase Dashboard
2. Перейди в **Database** → **Replication**
3. Убедись, что таблица `reports` есть в списке и Realtime включен

### 3. Альтернативный способ (через UI)

1. Открой Supabase Dashboard
2. Перейди в **Database** → **Tables**
3. Найди таблицу `reports`
4. Нажми на иконку шестеренки рядом с таблицей
5. Включи опцию **Enable Realtime**

### 4. Проверка работы

1. Открой приложение в двух разных браузерах (или вкладках)
2. Войди в обоих браузерах
3. В одном браузере внеси изменение
4. Во втором браузере в консоли (F12) должно появиться:
   - `📡 Получено обновление из Supabase: UPDATE`
   - `📥 Обновление данных через real-time:`
5. Данные должны обновиться автоматически во втором браузере

### 5. Проверка статуса подписки

В консоли браузера (F12) проверь:
- Должно быть: `✅ Подписка на изменения активна`
- Если видишь ошибки, проверь:
  - Выполнен ли SQL для включения Realtime
  - Правильно ли указаны переменные окружения
  - Доступна ли таблица reports

### 6. Возможные проблемы

**Подписка не работает:**
- Убедись, что выполнил SQL команду `ALTER PUBLICATION supabase_realtime ADD TABLE reports;`
- Проверь, что Realtime включен в настройках таблицы
- Проверь консоль браузера на наличие ошибок

**Данные не синхронизируются:**
- Убедись, что оба браузера используют один и тот же проект Supabase
- Проверь, что ID записи одинаковый (`main-reports`)
- Проверь логи в консоли обоих браузеров

**Ошибка "Realtime не настроен":**
- Проверь, что выполнил SQL команду
- Перезагрузи страницу после выполнения SQL

## После настройки

После выполнения SQL команды:
1. Обнови страницу в обоих браузерах
2. Проверь консоль - должна появиться запись `✅ Подписка на изменения активна`
3. Внеси изменение в одном браузере
4. Данные должны появиться автоматически во втором браузере
