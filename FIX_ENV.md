# Исправление ошибки конфигурации Supabase

## Проблема

Приложение выдает ошибку конфигурации Supabase, переменные окружения не загружаются.

## Решение

### Шаг 1: Проверь файл .env

Убедись, что файл `.env` находится в корне проекта (там же, где `package.json`).

Файл должен содержать:
```env
VITE_SUPABASE_URL=https://твой-проект.supabase.co
VITE_SUPABASE_ANON_KEY=твой-anon-ключ
```

**Важно:**
- Префикс `VITE_` обязателен!
- Без кавычек вокруг значений
- Без пробелов вокруг `=`
- Одна переменная на строку

### Шаг 2: Перезапусти dev сервер

**КРИТИЧЕСКИ ВАЖНО:** После изменения `.env` файла нужно перезапустить dev сервер!

1. Останови текущий dev сервер (Ctrl+C в терминале)
2. Запусти заново:
   ```bash
   npm run dev
   ```

Vite загружает переменные окружения только при запуске, поэтому изменения в `.env` не применяются без перезапуска!

### Шаг 3: Проверь консоль браузера

Открой консоль браузера (F12) и найди логи:
```
🔍 Проверка ключей Supabase:
  VITE_SUPABASE_URL: true (установлен)
  VITE_SUPABASE_ANON_KEY: true (установлен)
```

Если видишь `(НЕ УСТАНОВЛЕН)`, значит:
- Либо файл `.env` не найден
- Либо переменные не перезагружены (перезапусти dev сервер!)
- Либо неправильное имя переменной (нужен префикс `VITE_`)

### Шаг 4: Проверь формат .env файла

**Правильный формат:**
```env
VITE_SUPABASE_URL=https://abcdefgh.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Неправильный формат:**
```env
# ❌ Без префикса VITE_
SUPABASE_URL=https://...

# ❌ С кавычками
VITE_SUPABASE_URL="https://..."

# ❌ С пробелами
VITE_SUPABASE_URL = https://...

# ❌ Комментарий на той же строке (может работать, но лучше избегать)
VITE_SUPABASE_URL=https://... # комментарий
```

### Шаг 5: Получи ключи из Supabase

Если ключи отсутствуют:

1. Открой Supabase Dashboard: https://supabase.com/dashboard
2. Выбери свой проект
3. Перейди в **Project Settings** → **API**
4. Скопируй:
   - **Project URL** → это `VITE_SUPABASE_URL`
   - **anon public** ключ → это `VITE_SUPABASE_ANON_KEY`

### Шаг 6: Проверь расположение файла

Убедись, что файл `.env` находится в корне проекта:

```
fromi-crm/
├── .env              ← здесь!
├── package.json
├── vite.config.ts
├── services/
│   └── supabase.ts
└── ...
```

### Диагностика

Открой консоль браузера (F12) и проверь логи:

✅ **Если все правильно:**
```
🔍 Проверка ключей Supabase:
  VITE_SUPABASE_URL: true (установлен)
  VITE_SUPABASE_ANON_KEY: true (установлен)
```

❌ **Если что-то не так:**
```
🔍 Проверка ключей Supabase:
  VITE_SUPABASE_URL: false (НЕ УСТАНОВЛЕН)
  VITE_SUPABASE_ANON_KEY: false (НЕ УСТАНОВЛЕН)
❌ Supabase credentials not found!
```

### Частые ошибки

1. **"Переменные не загружаются"**
   - Решение: Перезапусти dev сервер (`npm run dev`)

2. **"Префикс VITE_ отсутствует"**
   - Решение: Добавь префикс `VITE_` к именам переменных

3. **"Файл .env не в корне проекта"**
   - Решение: Перемести файл в корень проекта

4. **"Пробелы вокруг ="**
   - Решение: Убери пробелы: `VITE_SUPABASE_URL=https://...`

5. **"Кавычки вокруг значений"**
   - Решение: Убери кавычки: `VITE_SUPABASE_URL=https://...`
