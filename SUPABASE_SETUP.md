# Настройка Supabase для FROMI CRM

Это руководство поможет тебе подключить Supabase для синхронизации данных в реальном времени.

## Шаг 1: Создание проекта в Supabase

1. Перейди на [supabase.com](https://supabase.com)
2. Зарегистрируйся или войди в аккаунт
3. Нажми "New Project"
4. Заполни:
   - **Name**: fromi-crm (или любое другое название)
   - **Database Password**: создай надежный пароль (сохрани его!)
   - **Region**: выбери ближайший регион
5. Нажми "Create new project" и подожди 1-2 минуты

## Шаг 2: Создание таблицы reports

1. В Dashboard Supabase перейди в **SQL Editor** (иконка слева)
2. Нажми "New query"
3. Скопируй весь SQL код из файла `supabase-schema.sql`
4. Вставь в редактор и нажми "Run" (или Ctrl+Enter)

Таблица `reports` будет создана с необходимой структурой и политиками безопасности.

## Шаг 3: Получение API ключей

1. В Dashboard перейди в **Project Settings** (шестеренка внизу слева)
2. Выбери вкладку **API**
3. Скопируй следующие значения:
   - **Project URL** (это будет `VITE_SUPABASE_URL`)
   - **anon public** ключ (это будет `VITE_SUPABASE_ANON_KEY`)

## Шаг 4: Настройка переменных окружения

1. В корне проекта создай файл `.env`:
   ```bash
   cp .env.example .env
   ```

2. Открой файл `.env` и заполни значения:
   ```env
   VITE_SUPABASE_URL=https://твой-проект.supabase.co
   VITE_SUPABASE_ANON_KEY=твой-anon-ключ
   ```

3. Замени значения на свои из Supabase Dashboard

## Шаг 5: Установка зависимостей и запуск

```bash
npm install
npm run dev
```

## Проверка работы

1. Открой приложение в двух разных браузерах или вкладках
2. Войди как разные пользователи (например, Алена и Денис)
3. Внеси изменения в данные в одном окне
4. Во втором окне изменения должны появиться автоматически через 1-2 секунды

## Важные заметки

- ⚠️ **anon key** безопасно использовать в клиентском коде - он ограничен политиками Row Level Security (RLS)
- 📦 Данные автоматически синхронизируются в реальном времени между всеми пользователями
- 💾 При отсутствии интернета используется fallback на localStorage
- 🔒 По умолчанию таблица доступна всем. Для продакшена настрой более строгие политики RLS

## Устранение проблем

### "Supabase не настроен"
- Проверь, что файл `.env` существует
- Убедись, что переменные начинаются с `VITE_`
- Перезапусти dev сервер после изменения `.env`

### "Ошибка при подключении к Supabase"
- Проверь правильность URL и ключа
- Убедись, что таблица `reports` создана
- Проверь политики RLS в Supabase Dashboard

### "Данные не синхронизируются"
- Проверь консоль браузера на наличие ошибок
- Убедись, что Realtime включен в Supabase (по умолчанию включен)
- Проверь, что подписка создана в коде

## Дополнительная настройка безопасности (опционально)

Для более строгой безопасности можно изменить политику RLS в SQL Editor:

```sql
-- Удалить старую политику
DROP POLICY IF EXISTS "Allow all operations" ON reports;

-- Создать новую для авторизованных пользователей
CREATE POLICY "Allow authenticated users" ON reports
  FOR ALL
  TO authenticated
  USING (true)
  WITH CHECK (true);
```

Это потребует настройки аутентификации в Supabase (выходит за рамки текущей задачи).
