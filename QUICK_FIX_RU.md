# ⚡ Быстрое исправление синхронизации

## 🎯 Проблема
Данные не синхронизируются между браузерами.

## ✅ Решение за 3 шага

### Шаг 1: Выполните SQL команду

1. Откройте: https://supabase.com/dashboard → ваш проект → **SQL Editor**
2. Вставьте и выполните:

```sql
-- Включаем Realtime для таблицы reports
ALTER PUBLICATION supabase_realtime ADD TABLE reports;

-- Проверяем, что всё работает
SELECT 
    'Realtime enabled' as status,
    count(*) as count
FROM pg_publication_tables 
WHERE pubname = 'supabase_realtime' 
  AND tablename = 'reports';
```

3. Должны увидеть: `count: 1` ✅

### Шаг 2: Включите Realtime в UI

1. В Supabase Dashboard: **Database** → **Replication**
2. Найдите таблицу `reports`
3. Включите переключатель **Realtime** (должен стать зеленым) 🟢

### Шаг 3: Проверьте работу

1. Откройте 2 браузера (или 2 вкладки в режиме инкогнито)
2. В обоих войдите в приложение
3. В первом браузере измените данные
4. Откройте консоль (F12) во втором браузере
5. Вы должны увидеть:
   ```
   ✅ Realtime подписка активна!
   📡 Получено событие из Supabase
   📥 Получено обновление из базы
   ```

## 🐛 Если не работает

### Проверка 1: Realtime подключен?

Откройте консоль (F12) и найдите:
- ✅ `✅ Realtime подписка активна!` - отлично!
- ❌ `❌ Ошибка Realtime канала` - выполните SQL команду из Шага 1 снова
- ❌ Вообще нет логов про Realtime - перезагрузите страницу

### Проверка 2: WebSocket работает?

1. Консоль браузера (F12) → вкладка **Network**
2. Фильтр: **WS** (WebSocket)
3. Должно быть соединение с `realtime` в URL
4. Статус: `101 Switching Protocols` ✅

### Проверка 3: Ключи Supabase правильные?

Откройте `services/supabase.ts` и сверьте:
- `supabaseUrl` с Settings → API → Project URL
- `supabaseAnonKey` с Settings → API → Project API keys → anon/public

## 📞 Нужна помощь?

Если всё ещё не работает:
1. Скопируйте все логи из консоли (с эмодзи: 📤, 📥, ❌, ✅)
2. Сделайте скриншот раздела Database → Replication
3. Отправьте мне

## 🎉 Готово!

После исправления **обязательно обновите страницу** (Ctrl+R) в обоих браузерах!

Данные должны синхронизироваться **мгновенно** (1-2 секунды).
