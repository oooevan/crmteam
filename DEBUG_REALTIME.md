# Диагностика Real-time синхронизации

## Проверка работы подписки

### 1. Проверь консоль браузера

В обоих браузерах открой консоль (F12) и проверь наличие этих сообщений:

**При загрузке страницы должно быть:**
- `🔔 Подписка на изменения данных в реальном времени...`
- `✅ Подписка на изменения активна`

**Если видишь ошибки:**
- `❌ Ошибка подписки на изменения` - проблема с настройками
- `❌ Таймаут при подписке на изменения` - проблема с сетью или настройками
- `⚠️ Канал подписки закрыт` - подписка не активна

### 2. Проверь включение Realtime в Supabase

1. Открой Supabase Dashboard
2. Перейди в **Database** → **Replication**
3. Проверь, есть ли таблица `reports` в списке
4. Если таблицы нет, выполни SQL:
   ```sql
   ALTER PUBLICATION supabase_realtime ADD TABLE reports;
   ```

### 3. Проверь структуру таблицы

1. Открой Supabase Dashboard → **Table Editor**
2. Открой таблицу `reports`
3. Проверь:
   - Колонка `id` должна быть типа `text` или `uuid`
   - Если `id` типа `uuid`, но используется строка `'main-reports'`, это может быть проблемой

### 4. Проверь данные в таблице

1. В **Table Editor** проверь запись с `id = 'main-reports'`
2. Убедись, что данные есть и структура правильная
3. Попробуй вручную изменить данные в таблице - должно появиться обновление в браузере (если подписка работает)

### 5. Тест сохранения данных

1. В одном браузере внеси изменение
2. Проверь консоль - должно появиться: `✅ Данные успешно сохранены в Supabase`
3. Проверь таблицу в Supabase Dashboard - данные должны обновиться
4. Во втором браузере проверь консоль - должно появиться: `📡 Получено обновление из Supabase: UPDATE`

### 6. Возможные проблемы

**Проблема 1: ID не совпадает**
- Если в таблице используется UUID, но код использует строку `'main-reports'`
- Решение: Измени схему таблицы на `id TEXT PRIMARY KEY` или используй правильный UUID

**Проблема 2: Realtime не включен**
- SQL команда не выполнена
- Решение: Выполни `ALTER PUBLICATION supabase_realtime ADD TABLE reports;`

**Проблема 3: Подписка не подключается**
- Проверь статус подписки в консоли
- Решение: Проверь переменные окружения и доступность Supabase

**Проблема 4: События не доходят**
- Подписка активна, но события не приходят
- Решение: Проверь фильтр в коде подписки
