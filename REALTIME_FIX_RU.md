# 🔧 Исправление синхронизации данных между браузерами

## Проблема
Данные, введенные в одном браузере, не отображаются в другом браузере в реальном времени.

## Причина
Realtime подписка в Supabase не активирована для таблицы `reports`.

## Решение (пошагово)

### Шаг 1: Выполните SQL команды в Supabase

1. Откройте ваш проект в Supabase Dashboard: https://supabase.com/dashboard
2. Перейдите в **SQL Editor** (левое меню)
3. Создайте новый запрос (кнопка "+ New query")
4. Скопируйте и вставьте содержимое файла `fix-realtime.sql`
5. Нажмите **Run** (или F5)

### Шаг 2: Проверьте результаты

После выполнения SQL скрипта вы должны увидеть:

- ✅ **Realtime enabled** с `publications_count: 1`
- ✅ Запись с `id = 'main-reports'` в таблице
- ✅ RLS политики настроены правильно

### Шаг 3: Включите Realtime в UI (если нужно)

1. В Supabase Dashboard перейдите в **Database** → **Replication**
2. Найдите таблицу `reports`
3. Убедитесь, что переключатель **Realtime** включен (зеленый)

### Шаг 4: Проверьте работу

1. Откройте приложение в двух разных браузерах (или в двух вкладках)
2. Войдите под любым пользователем (например, "Иван")
3. В первом браузере введите количество заявок для проекта
4. **В течение 1-2 секунд** данные должны появиться во втором браузере

### Шаг 5: Проверьте консоль браузера

Откройте консоль разработчика (F12) и проверьте логи:

**В первом браузере (где вы вводите данные):**
```
📤 Отправка в базу (Лидов: X)
🔄 handleUpdate ВЫЗВАН!
```

**Во втором браузере (где должны появиться данные):**
```
📥 Получено из базы (Лидов: X)
🎯 AdminDashboard рендерится
```

Если вы НЕ видите `📥 Получено из базы` во втором браузере, значит Realtime не работает.

## Дополнительная отладка

Если проблема не решилась, проверьте:

### 1. Правильность ключей Supabase

Откройте файл `services/supabase.ts` и проверьте:
- `supabaseUrl` - должен быть правильным URL вашего проекта
- `supabaseAnonKey` - должен быть правильным публичным ключом

Найти их можно в Supabase Dashboard → **Settings** → **API**

### 2. Проверьте настройки Realtime в коде

Откройте `services/storage.ts` и убедитесь, что:
- Функция `subscribeToDataChanges` вызывается в `App.tsx` (строка 987)
- Подписка не блокируется флагом `isSyncing`

### 3. Проверьте сеть

В консоли браузера перейдите на вкладку **Network** и проверьте:
- Есть ли WebSocket соединение с Supabase (ищите `realtime` в URL)
- Статус соединения должен быть `101 Switching Protocols`

## Альтернативное решение (если ничего не помогло)

### Вариант 1: Пересоздание таблицы

```sql
-- Удаляем таблицу
DROP TABLE IF EXISTS reports CASCADE;

-- Создаем заново из файла supabase-schema.sql
-- (скопируйте весь код из supabase-schema.sql)
```

### Вариант 2: Проверка WebSocket соединения

Добавьте в `services/supabase.ts`:

```typescript
export const supabase = createClient(supabaseUrl, supabaseAnonKey, {
  db: {
    schema: 'public',
  },
  auth: {
    persistSession: false,
    autoRefreshToken: false,
  },
  realtime: {
    params: {
      eventsPerSecond: 10,
    },
  },
});
```

## Проверка успешности

✅ **Синхронизация работает, если:**
- Данные появляются во втором браузере в течение 1-2 секунд после ввода
- В консоли второго браузера появляется `📥 Получено из базы`
- В Network видно активное WebSocket соединение

❌ **Синхронизация НЕ работает, если:**
- Данные появляются только после обновления страницы (F5)
- В консоли нет `📥 Получено из базы`
- WebSocket соединение отсутствует или закрывается

## Поддержка

Если проблема не решилась после всех шагов:
1. Откройте консоль браузера (F12)
2. Скопируйте все сообщения с эмодзи (📤, 📥, ❌, ✅)
3. Проверьте результаты SQL запроса из `fix-realtime.sql`

---

**Важно:** После применения исправлений **обязательно обновите страницу** (Ctrl+R или Cmd+R) в обоих браузерах!
