# 🧪 Тест ввода данных

## Задача: Понять, почему данные не попадают в state

Мне нужно увидеть **полный путь данных** от инпута до сохранения.

## Пожалуйста, сделайте следующее:

### Шаг 1: Подготовка
1. Обновите страницу (Ctrl+R или Cmd+R)
2. Войдите в приложение (под любым пользователем, например "Иван")
3. Откройте консоль (F12)
4. **ОЧИСТИТЕ консоль** (кнопка 🚫 или Ctrl+L)

### Шаг 2: Тест ввода
1. Найдите **любое** поле для ввода заявок (любой день недели)
2. Кликните в поле
3. Введите **одну цифру** (например, "5")
4. Подождите 1-2 секунды

### Шаг 3: Копирование логов
1. В консоли должны появиться логи
2. **Скопируйте ВСЕ логи** (Ctrl+A, затем Ctrl+C)
3. Отправьте мне

## Что я ищу в логах:

Должна быть такая последовательность:

```
🚨 INPUT onClick: ...
🚨 INPUT onFocus: ...
🚨 INPUT onKeyDown: { key: '5' }
🚨 INPUT onKeyUp: { value: '5' }
🚨 INPUT onChange ВЫЗВАН!
🚨 handleLeadChange ВЫЗВАН!
🔄 handleUpdate в TargetologistWorkspace вызван
🚨 onUpdateProjects вызван из App!
🚨 updateUserProjects ВЫЗВАН!
🔄 handleUpdate ВЫЗВАН!
📊 Данные ДО обновления: ...
🔄 Стейт обновлен: ...
```

## Если логов НЕТ или они не полные:

Это подскажет мне, где именно теряются данные:

- ❌ Нет `INPUT onClick/onFocus` → инпут не реагирует на клики
- ❌ Нет `INPUT onChange` → onChange не срабатывает
- ❌ Нет `handleLeadChange` → функция не вызывается
- ❌ Нет `handleUpdate` → callback не передается
- ❌ Нет изменений в стейте → данные теряются при обновлении

## Дополнительно: Проверьте инпут

Когда вы вводите цифру в поле:
- ✅ Цифра **появляется** в поле? (визуально видна)
- ❌ Цифра **НЕ появляется**? (поле остается пустым)

Это важно! Если цифра не появляется, значит инпут вообще не работает.

---

**Жду ваши логи!** 🔍
